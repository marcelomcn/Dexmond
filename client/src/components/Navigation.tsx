import React from 'react';
import { WalletConnect } from "@/components/dex/WalletConnect";

// This component is now deprecated as we moved the header to App.tsx
export default function Navigation() {
  return null;
}